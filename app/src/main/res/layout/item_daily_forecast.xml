<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:background="#80FFFFFF"
    android:gravity="center_vertical"
    android:padding="15dp"> <!-- Container for image and text -->

    <!-- ImageView on the left side -->
    <ImageView
        android:id="@+id/weatherIcon"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:contentDescription="Weather icon"
        android:layout_marginEnd="8dp" /> <!-- Add margin to separate image from text -->

    <!-- Vertical LinearLayout for TextViews -->
    <LinearLayout
        android:layout_width="262dp"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <TextView
            android:id="@+id/dateTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Date"
            android:textColor="#000000"
            android:textSize="18sp"
            android:textStyle="bold" />  <!-- Placeholder text for date -->

        <TextView
            android:id="@+id/tempRangeTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Temp Range"
            android:textColor="#000000" /> <!-- Placeholder text for temperature range -->

    </LinearLayout>

</LinearLayout>
